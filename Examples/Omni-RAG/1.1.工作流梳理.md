下面我按你描述的“两个 ReactAgent + 多存储入库”的思路，把**端到端工作流**和**跨页连续抽取（最难点）**用 mermaid 梳理出来：一张总流程图 + 一张“跨页产品抽取状态机/记忆更新”图 + 一张时序图。你拿去就能对齐研发拆任务。

---

```mermaid
flowchart TD
  A["租户上传PDF"]
  B["上传服务/网关 | 校验租户/权限/大小/格式"]
  C["保存原始PDF到阿里云OSS | raw_bucket/tenant_id/doc_id.pdf"]
  D["创建解析Job | job_id + doc_id + tenant_id | 写MySQL任务表"]

  E["Agent-1: PDF to Markdown 逐页解析 (ReAct)"]
  E1["下载PDF (OSS signed url)"]
  E2["逐页解析: 文本层优先 / 必要时OCR"]
  E3["每页产出Markdown: page_{n}.md + 结构元数据"]
  E4["写回OSS中间件备份: md_bucket/tenant/doc/page_n.md"]
  E5["写MySQL进度: page_done, quality, parser_used"]

  F["Agent-2: 逐页Markdown信息抽取 (ReAct + Memory)"]
  F1["读取 page_1..N.md (OSS)"]
  F2["记忆系统/状态管理: CompanyProfile + Products + 当前抽取上下文"]
  F3{"跨页连续性判断: 是否延续上一页同一对象?"}
  F4["继续补全同一产品/同一章节字段"]
  F5["收束上一个对象: 写入候选结构化结果"]
  F6["初始化新对象: 新产品 or 公司模块"]
  F7["字段抽取与校验: 规格/参数/卖点/性能等重点"]
  F8["冲突处理与去重: 同名产品/重复参数/单位统一"]

  G["抽取完成: 生成结构化JSON | company + products列表 + evidence"]

  H["入库编排器"]
  H1["MySQL: 结构化主表/版本/审计"]
  H2["ElasticSearch: 全文检索 原文chunk + 提取字段"]
  H3["Milvus: 向量 产品/段落 embedding"]
  H4["OSS: 归档 最终结构化JSON + 解析产物"]
  I["任务完成回调/通知"]

  A --> B --> C --> D
  D --> E --> E1 --> E2 --> E3 --> E4 --> E5
  E5 -->|下一页| E2
  E5 --> F --> F1 --> F2 --> F3
  F3 -->|延续| F4 --> F7 --> F8
  F3 -->|新对象/新章节| F5 --> F6 --> F7 --> F8
  F8 -->|下一页| F1
  F8 --> G --> H
  H --> H1
  H --> H2
  H --> H3
  H --> H4
  H --> I
```

---

## 2) 跨页“像人一样抽取产品信息”的核心：状态机 + 记忆更新（Mermaid）

这张图是你们 Agent-2 的核心逻辑：**每页读入 -> 判定延续/切换 -> 更新记忆 -> 收束/落盘**。

```mermaid
stateDiagram-v2
  [*] --> Init

  Init: 初始化记忆\ncompany_profile = {}\nproducts = {}\ncurrent_focus = None
  Init --> ReadPage

  ReadPage: 读取当前页Markdown\n提取线索(title/heading/表格/关键词/产品名)
  ReadPage --> Decide

  Decide --> ContinueSame: 延续上一页(同一产品/同一章节)
  Decide --> SwitchFocus: 切换焦点(新产品/新章节)
  Decide --> LowConfidence: 不确定(需要缓冲/等待下一页佐证)

  ContinueSame --> ExtractFields: 针对current_focus抽取\n规格/参数/卖点/性能/型号/图表说明
  SwitchFocus --> ClosePrev: 收束上一个对象\n补齐缺失字段/打上完成度\n写入products或company模块
  ClosePrev --> StartNew
  StartNew: 新建focus\nProduct{name/aliases}或CompanySection
  StartNew --> ExtractFields

  LowConfidence --> Buffer: 放入缓冲区\n记录候选focus与证据
  Buffer --> ReadNext

  ExtractFields --> MergeMemory: 合并到记忆\n单位统一/重复去重/冲突标注\n记录evidence(page_id, snippet)
  MergeMemory --> ReadNext

  ReadNext: page_index++
  ReadNext --> ReadPage: 还有下一页
  ReadNext --> Finalize: 已到最后一页

  Finalize: 收束current_focus\n输出结构化JSON\ncompany + products[] + evidence
  Finalize --> [*]
```

**你提到的难点（第3页识别到产品A，第4页继续产品A，同时判断是否出现别的产品）**，在这个状态机里对应的是 `Decide` 的三分支：

* `ContinueSame`：检测到“上页焦点仍然成立”（标题延续、表格未完、同型号/同产品名、‘续表/continued’、参数表紧接等）
* `SwitchFocus`：检测到“明显新产品/新章节”（新产品名/型号集合变化、新一级标题、目录结构跳转等）
* `LowConfidence`：模糊页（只有一些参数但没标题），先缓冲，等下一页标题或更多证据再回填

---

## 3) 端到端时序图（两段Agent + 四库入库）

```mermaid
sequenceDiagram
  autonumber
  participant U as Tenant/用户
  participant API as 上传/任务API
  participant OSS as 阿里云OSS
  participant DB as MySQL(任务+结构化)
  participant A1 as Agent-1(PDF->MD)
  participant A2 as Agent-2(抽取+Memory)
  participant ES as ElasticSearch
  participant MV as Milvus

  U->>API: 上传PDF(租户信息/元数据)
  API->>OSS: PUT raw PDF
  API->>DB: 创建job记录(job_id, doc_id, status=QUEUED)

  DB-->>A1: 拉取待处理job
  A1->>OSS: GET pdf(signed url)
  loop for each page
    A1->>A1: 解析第n页->Markdown
    A1->>OSS: PUT page_n.md(中间件备份)
    A1->>DB: 更新进度(page_done=n, quality, parser_used)
  end

  DB-->>A2: job进入EXTRACT阶段
  loop for each page md
    A2->>OSS: GET page_n.md
    A2->>A2: Decide(延续/切换/不确定)+更新Memory
  end
  A2->>A2: Finalize结构化结果JSON

  A2->>DB: 写company/products结构化表(带版本&审计)
  A2->>ES: bulk upsert(全文chunk + 字段索引)
  A2->>MV: upsert向量(产品/段落embedding)
  A2->>OSS: PUT final_result.json + artifacts manifest
  API-->>U: 返回处理完成/可查询
```

---

## 4) 你们的库怎么分工（建议的“最顺手”落点）

* **OSS**

  * raw PDF（原始）
  * 中间件：逐页 markdown + 解析元数据（比如 page_id、bbox、表格结构）
  * final_result.json（最终抽取结果归档）
* **MySQL（权威结构化事实库）**

  * job/状态机/幂等（job_id、版本、重试次数、失败原因）
  * company/profile（公司简介、资质、规模、优势）
  * product（产品主表 + 参数表 + 规格表 + 卖点表）
  * evidence（字段证据：page_n + snippet + offset）
* **ElasticSearch（检索与运营侧查询）**

  * doc/page/chunk 维度的全文检索
  * 把抽取字段也写进去，支持“按字段过滤 + 关键词搜索”
* **Milvus（语义召回）**

  * chunk 向量（段落级）
  * product card 向量（产品级摘要向量）
  * 支持“相似产品/问答式检索/语义搜索”

---

## 5) 我给你一个“Decide 跨页延续/切换”的判定清单（直接变成规则+提示词）

在 Agent-2 的 `Decide` 节点里，你们可以把信号分成三类（越靠前越强）：

**强信号：切换到新焦点**

* 出现新一级标题（如 `# 产品B`、`2.2 产品B`）
* 出现明显新产品名/新型号集合（且与 current_focus 差异大）
* 页眉/目录标识显示章节变化（如“产品介绍”->“资质认证”）

**强信号：延续同一焦点**

* 标题延续/重复（同产品名、同型号）
* 表格续表（“续表/continued/表X（续）”）
* 同一参数表在上一页未结束（上一页末尾是表格/冒号未完句/“……”）

**弱信号：不确定 -> Buffer**

* 只有参数、单位、数值，但缺少产品名/标题
* 图片说明/图注较多，正文少
* 扫描件OCR置信度低，结构不清晰

Buffer 的意义：先别做“错误切换”，宁可延迟一页再决定，然后回填。

---

如果你愿意，我还能继续把它压成一份**可直接落地的 Agent-2 Memory 结构（JSON schema）+ 入库表结构映射 + ES index mapping** 的 mermaid/草案（不问你任何额外问题，我会按通用企业产品手册的字段设计直接给一版）。
