## Intro




```sh
pip install --upgrade "langgraph-cli[inmem]"
pip install "langgraph-cli[inmem]"
```


```sh
langgraph new ./
# 然后选择 1 创建 LangGraph 项目, 接着选择 1 Python语言

# 然后在根目录执行
pip install -e .
```

- 工具集: https://python.langchain.com/docs/integrations/tools/